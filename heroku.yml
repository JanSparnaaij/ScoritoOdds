build:
  docker:
    web: Dockerfile

run:
  web: sh -c "redis-server --daemonize yes && hypercorn --bind 0.0.0.0:${PORT:-8000} app:create_app()"
  worker: celery -A app.celery_worker worker --loglevel=info
